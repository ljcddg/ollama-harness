# The npmmirror registry serves a malformed packument for `fsevents` that makes
# npm 10.9.7 crash with "Invalid Version:" during arborist's dedupe pass.
# Official registry is slower but resolves cleanly.
registry=https://registry.npmjs.org/
